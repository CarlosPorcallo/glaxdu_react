// trayendo la mock data
import {caption, cards} from '../../../config/teacher';

// sección home
export const getCaptionTeacher = () => {
    return caption;
};

export const getCardsTeacher = () => {
    return cards;
};